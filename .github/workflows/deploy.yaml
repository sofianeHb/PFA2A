name: Deploy Pipeline

on:
  workflow_run:
    workflows: ["ML Pipeline"]
    types:
      - completed
#Permission to download from another workflow
permissions:
  actions: read
  contents: read

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Download mlruns from previous workflow
      uses: dawidd6/action-download-artifact@v2
      with:
        workflow: ml_pipeline.yml
        workflow_conclusion: success
        run-id: ${{ github.run_id }}
        name: mlruns
        path: mlruns/
        github-token: ${{ github.token }}
        check_artifacts: true

    - name: Download outputs from previous workflow
      uses: dawidd6/action-download-artifact@v2
      with:
        workflow: ml_pipeline.yml
        workflow_conclusion: success
        run_id: ${{ github.event.workflow_run.id }}
        name: outputs
        path: outputs/
        github_token: ${{ secrets.GITHUB_TOKEN }}
        check_artifacts: true

    - name: Install Docker Compose
      run: |
        sudo apt-get update
        sudo apt-get install -y docker-compose

    - name: Build and run containers
      run: |
        docker-compose up -d --build
        sleep 10


    - name: Download and set up Ngrok
      run: |
        wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-stable-linux-amd64.zip
        unzip ngrok-stable-linux-amd64.zip
        sudo mv ngrok /usr/local/bin
        ngrok config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

    - name: Start Ngrok tunnels
      run: |
        nohup ngrok http 8000 > ngrok_fastapi.log &
        nohup ngrok http 5000 > ngrok_mlflow.log &
        nohup ngrok http 3000 > ngrok_grafana.log &   

    - name: Show Ngrok URLs from logs
      run: |
        echo "FastAPI URL:"
        grep -o 'https://[0-9a-z]*\.ngrok\.io' ngrok_fastapi.log | head -1
        echo "MLflow URL:"
        grep -o 'https://[0-9a-z]*\.ngrok\.io' ngrok_mlflow.log | head -1
        echo "Grafana URL:"
        grep -o 'https://[0-9a-z]*\.ngrok\.io' ngrok_grafana.log | head -1


    - name: Wait
      run: |
        sleep 60
